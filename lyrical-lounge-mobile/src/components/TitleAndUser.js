import { View, Text, TouchableOpacity, Image, StyleSheet, ScrollView } from 'react-native';

const styles = StyleSheet.create({

})

function Title(){
    return (
        <View>
            <Text>You are logged in as: </Text>
            <Text> Lyrical Lemonade</Text>
     </View>
    )
};

export default Title;